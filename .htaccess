# letmetellyouastory.org — Apache/LiteSpeed
DirectoryIndex index.html index.htm
Options -Indexes -MultiViews
AddType application/manifest+json .webmanifest
<IfModule mod_brotli.c>
  AddOutputFilterByType BROTLI_COMPRESS text/html text/css text/javascript application/javascript application/json image/svg+xml text/plain application/xml
</IfModule>
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json image/svg+xml text/plain application/xml
</IfModule>
<IfModule mod_headers.c>
  <FilesMatch "\.(woff2|js|css|png|jpg|jpeg|webp|avif|svg|ico)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  <FilesMatch "\.html$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>
  <FilesMatch "(sitemap\.xml|robots\.txt|llms\.txt|llms-full\.txt)$">
    Header set Cache-Control "public, max-age=3600"
  </FilesMatch>
  <Files "sw.js">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </Files>
  <Files "manifest.webmanifest">
    Header set Cache-Control "public, max-age=86400"
  </Files>
  Header unset X-Powered-By
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  Header set X-Content-Type-Options "nosniff"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Permissions-Policy "geolocation=(), microphone=(), camera=(), interest-cohort=()"
  Header set Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; font-src 'self'; connect-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; object-src 'none'"
</IfModule>
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} off
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
  RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
  RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]

  # Legacy permalinks from the old WordPress site, mapped to the letter that
  # replaced them. Search Console was reporting these as 404s with real inbound
  # history, so they resolve instead of dying. Only mappings we can evidence:
  # the old slug names the letter it points at.
  RewriteRule ^final-letter-on-dreams-and-peace/?$ /letters/on-dreams-and-peace/ [L,R=301]
  RewriteRule ^letter-25-on-grief-and-loss/?$ /letters/on-grief-and-loss/ [L,R=301]
  RewriteRule ^final-letter/?$ /letters/on-dreams-and-peace/ [L,R=301]
</IfModule>
ErrorDocument 404 /404.html
